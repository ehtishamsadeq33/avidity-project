Fix the remaining PDF issues in generatePDFHTML. Do NOT change 
anything about screen display, calculations, MongoDB or navigation.

═══════════════════════════════════════════════════════
🔹 PROBLEM 1: SUB-BAR VALUES SHOWING 0.00 ON PAGE 2
═══════════════════════════════════════════════════════

The sub-bars for Skills, Will and Environmental Support are all 
showing 0.00 because the field names used in the PDF HTML do not 
match the actual field names in the composites object.

Do this first: console.log(JSON.stringify(composites)) right before 
generatePDFHTML is called and check the exact field names that exist 
on the composites object for skills, will and environmentalSupport.

Then update the subBar() calls in the HTML to use EXACTLY those 
field names. Do not guess — read the actual composites object 
structure from the console log and map them correctly.

For example if the actual fields are named like:
  composites.skills.skillScore
  composites.skills.knowledgeScore  
  composites.skills.behaviorScore

Then use those exact names. The fix is purely matching the field 
names in the PDF to what actually exists in the composites object.

Also make sure each value passed to subBar() is a Number. 
If the value might be undefined, fall back to 0:
  composites.skills.skillScore ?? 0

═══════════════════════════════════════════════════════
🔹 PROBLEM 2: PAGE 3 LEFT AND RIGHT SHOW SAME PERCENTAGE
═══════════════════════════════════════════════════════

Currently both the left pill (Perceived) and right pill (Actual) 
show the same percentage. This means composites.skills.perceived 
(or whatever the perceived field is called) is undefined, so it 
falls back to the actual percent.

Do this: console.log the full composites object and find the exact 
field name that stores the PERCEIVED score for each composite.

The perceived score comes from the "What They Think" section — 
it is the score the user self-reported before the actual calculated 
score. Find where this is stored and use that exact field name for 
leftPct in reportPage3Data.

If perceived scores are stored separately (e.g. in perceivedActual 
or a different object passed to ReportPage3), then pass that object 
into generatePDFHTML and use it to populate leftPct.

Also fix the symbol between the two pills — instead of a static 
↔ symbol, compute it properly:
  symbol = leftPct > rightPct ? '▲' : leftPct < rightPct ? '▼' : '='

Where ▲ means perceived is higher than actual (overconfident),
▼ means perceived is lower than actual (underestimating),
= means they match.

═══════════════════════════════════════════════════════
🔹 PROBLEM 3: BLANK PAGES (PAGE 2 AND PAGE 5)
═══════════════════════════════════════════════════════

There are empty pages being generated. This is caused by 
page-break-after: always being applied to the last div before 
an empty section, creating a trailing blank page.

Fix by:
1. Only use page-break-after: always on pages that are NOT the 
   last page of content
2. Change the last content div to use class="last-page" with 
   NO page-break-after
3. Remove any empty <div> or <br> tags between page sections 
   that might be creating ghost pages
4. Make sure there is no trailing page-break-after on the 
   Page 3 / last-page div

The correct page-break structure should be:
  Page 1 div → page-break-after: always  ✓
  Page 2 div → page-break-after: always  ✓
  Page 3 div → NO page-break (last page) ✓

═══════════════════════════════════════════════════════
🔹 PROBLEM 4: PAGE 2 APPEARS BLANK / OUT OF ORDER
═══════════════════════════════════════════════════════

Based on the PDF, Page 2 is blank and the composite breakdown 
content is appearing on Page 3 instead. This means the page 
ordering in the HTML is wrong.

Correct page order must be:
  Page 1: Candidate info + Perceived vs Actual + Composites summary 
           + CRA Score Spread
  Page 2: Total CRA Score + Skills Composite breakdown with sub-bars 
           + Will Composite breakdown + Environmental Support breakdown 
           + Directive vs Non-Directive bar
  Page 3: "What do your results mean?" with perceived vs actual 
           comparison pills and statement cards for each composite

Make sure the HTML divs are in exactly this order and that 
Page 2 content is not accidentally inside a page-break or 
hidden by CSS.

═══════════════════════════════════════════════════════
🔹 SUMMARY OF EXACT CHANGES NEEDED
═══════════════════════════════════════════════════════

1. console.log(composites) before calling generatePDFHTML
2. Fix all subBar field names to match actual composites object keys
3. console.log to find perceived score field names
4. Fix leftPct in reportPage3Data to use real perceived values
5. Fix symbol to be ▲ ▼ or = based on comparison
6. Fix page ordering so Page 2 has the composite breakdowns
7. Remove trailing blank pages by fixing page-break-after on 
   last page div
8. Add ?? 0 fallback on every subBar value to prevent 0.00

DO NOT change screen display, calculations, MongoDB, navigation 
or any other screens.