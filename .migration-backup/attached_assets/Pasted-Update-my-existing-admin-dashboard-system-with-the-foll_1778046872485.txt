Update my existing admin dashboard system with the following changes and fixes.

---

## ❌ REMOVE EXPORT FEATURE COMPLETELY

* Remove Export tab
* Remove all backend routes related to export (CSV, Excel, ZIP)
* Remove any UI buttons related to export

---

## ✅ CANDIDATES TAB — ADD VIEW BUTTON

Currently:

* There is a yellow "Download PDF" button

### Add:

* A **"View" button** next to Download

### Behavior:

* When admin clicks **View**:

  * Open full report preview screen
  * Show ALL report pages:

    1. Intro page
    2. Page 1
    3. Page 2
    4. Page 3
  * Render using existing `generateReportHTML`
  * Display inside scrollable view (mobile-friendly)

### Important:

* DO NOT generate PDF for viewing
* Render HTML directly in a webview or container

---

## 📊 OVERVIEW TAB — CHART FIXES

### 1. Score Distribution Chart

Problem:

* There is unwanted empty space on the LEFT side

Fix:

* Chart must start from the Y-axis (no padding on left)
* Remove left margin / padding
* Ensure bars align directly with Y-axis

If using Chart.js:

* set `layout.padding.left = 0`
* set `scales.x.offset = false`
* ensure `categoryPercentage = 1.0`
* ensure `barPercentage = 1.0`

---

### 2. Gender Distribution Chart

Problems:

* Empty space on LEFT
* Y-axis labels are broken (e.g., 0,0,1,1,1)

Fix:

* Remove left padding/margin
* Fix Y-axis tick generation

If using Chart.js:

* set:
  ticks: {
  stepSize: 1,
  precision: 0
  }

* ensure values are integers (count of Male/Female)

* also:
  beginAtZero: true

---

## 📱 MOBILE UI IMPROVEMENTS

### Candidates List Row:

Each row should have:

* Name
* Key scores
* Buttons:
  [View] [Download]

Layout:

* Buttons aligned right
* Compact card design

---

## 🧠 REPORT VIEW SCREEN

Create a new screen:

### "Report Preview Screen"

* Scrollable vertical layout
* Render full HTML report
* Mobile responsive
* Include:

  * Logo
  * All charts
  * All pages

---

## ⚙️ BACKEND (if needed)

No major change required except:

* Ensure endpoint exists:
  GET /admin/candidate/:id

Frontend uses this data to render HTML report

---

## ✅ FINAL STRUCTURE

Tabs should now be:

1. Overview
2. Candidates
3. Analytics
4. Settings

(Export removed)

---

## 🎯 OUTPUT REQUIRED

* Updated frontend components
* Updated chart configs (fix spacing + ticks)
* New "View Report" screen
* Updated Candidates list UI
* Clean, working code
